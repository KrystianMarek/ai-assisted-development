
# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# Claude Code per-user local settings (permissions, etc.)
.claude/settings.local.json

# Agent conversation memory (per-machine, not shared)
.remember/
